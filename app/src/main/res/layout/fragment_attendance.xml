<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".view.attendance.AttendanceFragment">

    <fragment
        android:id="@+id/map_attendance"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.example.mapwithmarker.MapsMarkerActivity" />

    <include
        android:layout_width="224dp"
        android:layout_height="wrap_content"
        layout="@layout/current_time_dialog"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/tab_get_current_location"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_anchor="@id/layout_bottom_sheet"
        app:layout_anchorGravity="top|end"
        app:backgroundTint="@color/white"
        app:tint="@color/colorPrimary"
        android:layout_margin="20dp"
        android:translationY="-40dp"
        android:src="@drawable/ic_baseline_location_searching_24"/>
    <include
        android:id="@+id/layout_bottom_sheet"
        layout="@layout/button_sheet_attendance"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>